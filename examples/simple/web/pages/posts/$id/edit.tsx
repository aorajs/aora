import React from 'react';
import styles from '../index2.module.less'

const PostItemDetail = () => {
  // const { state, dispatch } = useContext<IContext<IData>>(STORE_CONTEXT);
  return <div className={styles.edit}>Post Item Detail</div>;
};

export default PostItemDetail;
